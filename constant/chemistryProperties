FoamFile { version 2.0; format ascii; class dictionary; location "constant"; object chemistryProperties; }

chemistryType
{
    solver            ode;
}

chemistry       on;
initialChemicalTimeStep 1e-9;

odeCoeffs
{
    solver          seulex;     // robust stiff integrator for H2/O2 chemistry
    absTol          1e-10;
    relTol          1e-5;
}

#include "reactions"
