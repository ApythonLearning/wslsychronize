type            sets;
libs            ("libsampling.so");
interpolationScheme cellPoint;
setFormat       raw;
sets
(
    radial
    {
        type    lineUniform;
        axis    x;
        start   (0.0001 0.0001 0.0001);
        end     (0.0249 0.0001 0.0001);
        nPoints 50;
    }
);
fields (OH);
